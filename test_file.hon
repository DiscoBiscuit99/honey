# constant variable declaration
const a: number = 10;

# mutable variable declaration
let b: int = 20;

fn main(): nil {

  # some block testing
  {
    let x: number = 0;
    let y: number = 0;
  }

  let a: int = 0;

  {
    const b: number = 0;
    a + b
  }
}

# todo! (closures)
#const f: (n: number) -> nil = {
#  # body of function `f`
#};

# const Pet: struct = {
#   name: string,
#   age: int,
# };
